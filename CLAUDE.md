# CLAUDE.md

このファイルはClaude Codeによる実装作業のためのプロジェクトコンテキストです。要件の詳細は `requirements.md` を参照してください。

## プロジェクト概要

Shelf Drop（仮称）: Windows / macOS 両対応のファイル一時置き場（シェルフ）＋クリップボード履歴管理アプリ。Edge Drop（Windows向けクリップボードマネージャー）、Yoink（Mac向けシェルフアプリ）を参考に、無料・クロスプラットフォームで自作する。

## 技術スタック

- **フロントエンド**: Svelte 5 + TypeScript
- **バックエンド**: Rust（Tauri v2）
- **データ永続化**: SQLite（rusqlite）
- **クリップボード抽象化**: arboard または clipboard-rs
- **グローバルホットキー**: tauri-plugin-global-shortcut
- **Windows D&D**: windows-rs（COM / IDropTarget）
- **macOS D&D**: NSPasteboard / NSDraggingSession（Tauri経由）

## ディレクトリ構成（想定）

```
shelf-drop/
├── src/                    # Svelteフロントエンド
│   ├── lib/
│   │   ├── components/     # Shelf.svelte, ClipboardHistory.svelte, Settings.svelte 等
│   │   ├── stores/         # シェルフ状態・クリップボード履歴のストア
│   │   └── types/          # 型定義（Rust側とインターフェースを合わせる）
│   └── routes/ or App.svelte
├── src-tauri/              # Rustバックエンド
│   ├── src/
│   │   ├── clipboard/      # クリップボード監視（OS分岐）
│   │   ├── drag_drop/      # ドラッグ&ドロップ処理（OS分岐）
│   │   ├── storage/        # SQLiteアクセス層
│   │   ├── shortcut/       # グローバルホットキー
│   │   └── main.rs
│   └── tauri.conf.json
├── requirements.md
└── CLAUDE.md
```

## 実装フェーズ（requirements.md 7章に対応）

現在のフェーズをここに随時更新すること。

- [ ] Phase 1（MVP）: 常駐シェルフ + ファイルD&D格納（F-01〜F-05, F-18, F-20）
- [ ] Phase 2: クリップボード履歴 + ピン留め（F-11〜F-13, F-16）
- [ ] Phase 3: Windows側自動検出、表示設定、自動起動（F-08(Win), F-06, F-07, F-10, F-19）
- [ ] Phase 4: 検索・タグ・スタック（F-14, F-15, F-17）
- [ ] Phase 5: macOS側自動検出、同期検討（F-08(Mac), F-22）

各フェーズはrequirements.mdの機能IDに対応させ、実装完了時にチェックを更新する。

## コーディング規約

- コード内コメントは日本語、ライブラリ名・技術用語は英字表記のまま（カタカナ化しない）
- Svelte側は`<script lang="ts">`必須、型を明示する
- Rust側は`clipboard`/`drag_drop`/`storage`/`shortcut`の各モジュールをOS分岐込みで独立させ、`#[cfg(target_os = "windows")]` / `#[cfg(target_os = "macos")]`で分離する
- OS依存コードは可能な限りtraitで抽象化し、共通インターフェース越しに呼び出す
- コミット前にフェーズのチェックリスト（本ファイル）を更新する

## 既知のリスク・注意点（requirements.md 8章より抜粋）

- macOSのグローバルドラッグ検知（F-08）はAccessibility APIまたはprivate API依存の可能性が高く、Mac App Store配布ではなくnotarization経由の直接配布を前提とする
- クリップボード監視はmacOS側でポーリング方式（数百ms間隔）になるため、バッテリー消費を検証すること
- Windowsのセキュリティソフトがグローバルフックを誤検知する可能性があるため、コード署名を検討する

## 開発コマンド（seed、実装開始時に更新）

```bash
# 開発サーバー起動
npm run tauri dev

# ビルド
npm run tauri build
```

---

## サブエージェント委任方針

タスクの複雑さを判断し、以下の基準でTask toolによるサブエージェント委任を行うこと。

機械的・定型的な作業（単純なファイル探索、フォーマット変換、定型的なコード生成、ドキュメントの下読み・要約、テストの雛形作成など）は、Task toolでサブエージェントに委任し、可能な限り自身のコンテキストを消費しないこと。

設計判断・アーキテクチャ選定・デバッグ方針の決定など、判断力が必要な作業は自身で直接処理すること。

委任するか迷った時点で、原則は委任する（グローバル `~/.claude/CLAUDE.md` と同じ）。委任しない場合はそれが例外であり、理由を一言添える。

これはコンテキストを優先する明示的な選択である。総トークンは増える。1回のspawnには約2万トークンの固定費がかかり（エージェントが自分のシステムプロンプトとCLAUDE.mdを読み直すため）、`make check` を回すだけでも同じだけ乗る。それを承知のうえでコンテキストを取る、というのがこの方針である。「コストが増えるから委任をやめる」方向に読み替えないこと。

ただし並行で2本以上走らせない。セッションの利用上限に当たると、走行中のエージェントが書きかけのまま落ちる（2026-08-01に実際に2本失った）。コストは許容するが、作業が消えるのは別問題である。長い実装は1本ずつ。

調査フェーズを自分で始めない。セッション開始時やタスク着手時の状況把握（「どこに何があるか」「残作業は何か」「生成物が置かれているか」の確認）は、コードなら `codebase-explorer`、`docs/` なら `docs-reader` へ最初に委任する。設計判断はその報告を読んでから行う。自分で広域grepを始めた時点で既定を外している。

例外（自分でやってよい）は、1〜2箇所の軽微な編集・単発のgrepや1ファイル確認など、委任の往復コストの方が明らかに高い作業に限る。設計判断のためのソース通読は委任しない（読んで気付くことが成果物なので、要約されると失われる）。

委任先はすべて `.claude/agents/`（リポジトリ内）にある。個人の `~/.claude/agents/` に同名の定義があっても、`.claude/agents/` の方が優先される（Claude Codeの仕様。プロジェクト定義がユーザー定義より上位で、この順位は設定で変えられない）。リポジトリだけをcloneした環境でも同じ挙動になるのはこのため。

| 名前 | 用途 | 権限 | モデル |
|---|---|---|---|
| codebase-explorer | コードの位置特定（関数・定数・TODO(タグ)の所在） | 読み取りのみ | haiku |
| docs-reader | docs/の下読み・必要な節の要約。根拠を文書名＋節番号で返す | 読み取りのみ | haiku |
| test-scaffolder | 既存の流儀に沿ったテスト雛形の追加。プロダクションコードは触らない | テストファイルの読み書き | haiku |
| check-runner | make check等を実行し、失敗箇所だけを圧縮して報告 | 実行・読み取り（修正はしない） | haiku |
| implementer | 方針確定後の実装（新規ファイル・機能追加・リファクタリング） | 読み書き・実行 | 継承 |
| bug-hunter | エラー・テスト失敗・スタックトレースの原因調査。修正はしない | 読み取り・実行 | 継承 |

モデルの「継承」は意図的。読み取り専用の4つは `model: haiku` で固定しているが、implementer / bug-hunterはfrontmatterのmodelを書いていないので呼び出し元のモデルを継ぐ。この2つは失敗が静かに通る（コンパイルは通るが設計を外したコード／それらしいが違う原因）ため、読み取り専用の探索と同じ扱いにしない。

実装とそのテストを同時に入れる場合は、test-scaffolderに分けずimplementerへ一体で渡してよい（テストが実装と密結合なため）。既存テストへの追加だけならtest-scaffolder。

### 検索ツールの使い分け

コード／文書探索で、対象語が分かっているかどうかを基準に使い分けること。

- 語や記号が分かっている（関数名・定数・TODO(タグ)・決定番号・スパイク名などの固定文字列）：Grep / Glob、または委任先のcodebase-explorer / docs-reader。完全一致で取りこぼしが無い。
- 語彙が分からず、概念や意図から辿りたい（「〇〇のリスクについて書いてある箇所」のような言い換え検索）：local-rag（MCP）のsearch_codebase（意味検索）。ただし固有ラベルを含む問いには弱く、top_kを広げても本命が出ないことがある。結果に既知の決定番号やファイル名が含まれない場合はGrepで裏取りする。
- 迷ったら両方使ってよい。RAGは概念で当たりを付ける一次探索、Grepは確定・裏取りという役割分担にする。
- codebase-explorer / docs-readerもsearch_codebaseを使える。委任文に「語彙が分からない」ことが分かっている場合はその旨を書く。
